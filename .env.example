# ─── JWT ─────────────────────────────────────────────────────────────────────
# Clave pública RS256 del Auth Service para validación de tokens.
# Obtener desde: cat <ruta-auth>/src/main/resources/publicKey.pem
# En Fly.io NO va aquí — usar: fly secrets set JWT_PUBLIC_KEY="$(cat publicKey.pem)"
JWT_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----
...
-----END PUBLIC KEY-----"
